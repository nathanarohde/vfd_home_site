import React from 'react';
import Display from '../../Containers/Display/Display'

const main = (props) => {
  return (
    <main className="Wrapper">
      <Display/>
    </main>
  )
}

export default main;
